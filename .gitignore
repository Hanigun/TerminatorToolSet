# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# generated build artifact (compiler/embed_assets.py, п.6.1 ТЗ)
terminator_toolset/_embedded_data.py
# Возвраты для индексатора codebase-memory: весь проект должен быть
# проиндексирован, поиск внутри файлов ведётся через search_code, а не grep.
# Бинарники (*.db, 7z/, exe) остаются исключёнными — в них нет индексируемого содержимого.
!terminator_toolset/_embedded_data.py
!spawns.json
!TERMINATOR_OVERHAUL.json
!list.txt
!boot.log
!config.json
!_roundtrip_out.xml
!configs/
!Logs/
!UprisingCustomPresets/
!UprisingCustomRandomizer/
!uprising_backups/

# Инструменты проекта (индексация/состояние агентов)
.codegraph/
.unlazy/

# Артефакты сборки / упаковки (живут в ../compiler)
build/
dist/
../compiler/build/
../compiler/dist/

# Логи и рантайм-данные рядом с программой
boot.log
Logs/
configs/
*.db
config.json

# Тестовый вывод round-trip
_roundtrip_out.xml

# Внешние утилиты и рабочие заметки (не входят в проект)
# исключение: ассеты карты Uprising — версионируются shields, difficulty
# и текстура карты (остальное дерево UprisingMap/ локально: CustomImages и т.п.)
# shipped map assets are part of the program: tracked (only the
# runtime-generated CustomImages cache above stays ignored)
# --- not part of the program: dev docs, plans, gates, tests ---
ADR-001-project-peculiarities.md
GAMEASSETS_PLAN.md
UPRISING_RANDOMIZER_V2_PLAN.md
NEXUS_DESCRIPTION.txt
GATES.md
tests/
TestProject.json
scripts/
run_dev.bat
AGENTS.md

# Кэш конвертера DDS->WebP: генерируется локально по слоям
# (BaseGame/проект/мод), в репозиторий не входит
assets/CustomImages/
SWT Editor.exe
list.txt

# Пользовательские данные рядом с программой (бэкапы карт, кастом-пресеты,
# сессии открытых файлов с абсолютными путями локальной машины)
uprising_backups/
UprisingCustomPresets/
UprisingCustomRandomizer/
TERMINATOR_OVERHAUL.json
spawns.json
